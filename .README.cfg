The command-line command 'config' is aliased to git, using the "~/.cfg" directory for
all storage.  I'm using it to keep my dotfiles versioned.  So 'config status', etc.,
works just like 'git status', but for that directory.
